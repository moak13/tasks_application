import 'package:flutter/material.dart';

class TeamsMobileExtraLargeView extends StatelessWidget {
  const TeamsMobileExtraLargeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Teams Mobile Extra Large View'),
      ),
    );
  }
}
