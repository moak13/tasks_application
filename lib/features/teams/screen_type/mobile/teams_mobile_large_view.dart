import 'package:flutter/material.dart';

class TeamsMobileLargeView extends StatelessWidget {
  const TeamsMobileLargeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Teams Mobile Large View'),
      ),
    );
  }
}
