import 'package:flutter/material.dart';

class TeamsMobileSmallView extends StatelessWidget {
  const TeamsMobileSmallView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Teams Mobile Small View'),
      ),
    );
  }
}
