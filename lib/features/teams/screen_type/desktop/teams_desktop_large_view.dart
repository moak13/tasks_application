import 'package:flutter/material.dart';

class TeamsDesktopLargeView extends StatelessWidget {
  const TeamsDesktopLargeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Teams Desktop Large View'),
      ),
    );
  }
}
