import 'package:flutter/material.dart';

class TeamsDesktopNormalView extends StatelessWidget {
  const TeamsDesktopNormalView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Teams Desktop Normal View'),
      ),
    );
  }
}
