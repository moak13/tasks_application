import 'package:flutter/material.dart';

class TeamsDesktopSmallView extends StatelessWidget {
  const TeamsDesktopSmallView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Teams Desktop Small View'),
      ),
    );
  }
}
