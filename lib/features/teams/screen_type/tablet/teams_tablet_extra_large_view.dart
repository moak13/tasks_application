import 'package:flutter/material.dart';

class TeamsTabletExtraLargeView extends StatelessWidget {
  const TeamsTabletExtraLargeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Teams Tablet Extra Large View'),
      ),
    );
  }
}
