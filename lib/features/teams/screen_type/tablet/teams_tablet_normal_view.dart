import 'package:flutter/material.dart';

class TeamsTabletNormalView extends StatelessWidget {
  const TeamsTabletNormalView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Teams Tablet Normal View'),
      ),
    );
  }
}
