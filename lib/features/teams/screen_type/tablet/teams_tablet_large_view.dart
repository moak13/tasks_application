import 'package:flutter/material.dart';

class TeamsTabletLargeView extends StatelessWidget {
  const TeamsTabletLargeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Teams Tablet Large View'),
      ),
    );
  }
}
