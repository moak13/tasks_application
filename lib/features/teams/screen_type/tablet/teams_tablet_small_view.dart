import 'package:flutter/material.dart';

class TeamsTabletSmallView extends StatelessWidget {
  const TeamsTabletSmallView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Teams Tablet Small View'),
      ),
    );
  }
}
