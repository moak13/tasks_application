import 'package:flutter/material.dart';

class TasksMobileSmallView extends StatelessWidget {
  const TasksMobileSmallView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Tasks Mobile Small View'),
      ),
    );
  }
}
