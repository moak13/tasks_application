import 'package:flutter/material.dart';

class TasksMobileNormalView extends StatelessWidget {
  const TasksMobileNormalView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Tasks Mobile Normal View'),
      ),
    );
  }
}
