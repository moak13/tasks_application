import 'package:flutter/material.dart';

class TasksTabletNormalView extends StatelessWidget {
  const TasksTabletNormalView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Tasks Tablet Normal View'),
      ),
    );
  }
}
