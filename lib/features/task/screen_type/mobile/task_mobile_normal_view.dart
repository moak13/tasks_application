import 'package:flutter/material.dart';

class TaskMobileNormalView extends StatelessWidget {
  const TaskMobileNormalView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Task Mobile Normal View'),
      ),
    );
  }
}
