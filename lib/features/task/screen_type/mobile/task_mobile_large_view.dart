import 'package:flutter/material.dart';

class TaskMobileLargeView extends StatelessWidget {
  const TaskMobileLargeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Task Mobile Large View'),
      ),
    );
  }
}
