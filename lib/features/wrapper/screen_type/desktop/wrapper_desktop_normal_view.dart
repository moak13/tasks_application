import 'package:flutter/material.dart';

class WrapperDesktopNormalView extends StatelessWidget {
  const WrapperDesktopNormalView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Wrapper Desktop Normal View'),
      ),
    );
  }
}
