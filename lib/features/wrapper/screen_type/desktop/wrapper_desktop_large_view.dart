import 'package:flutter/material.dart';

class WrapperDesktopLargeView extends StatelessWidget {
  const WrapperDesktopLargeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Wrapper Desktop Large View'),
      ),
    );
  }
}
