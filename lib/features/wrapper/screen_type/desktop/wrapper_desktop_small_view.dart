import 'package:flutter/material.dart';

class WrapperDesktopSmallView extends StatelessWidget {
  const WrapperDesktopSmallView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Wrapper Desktop Small View'),
      ),
    );
  }
}
