import 'package:flutter/material.dart';

class WrapperMobileSmallView extends StatelessWidget {
  const WrapperMobileSmallView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Wrapper Mobile Small View'),
      ),
    );
  }
}
