import 'package:flutter/material.dart';

class WrapperMobileNormalView extends StatelessWidget {
  const WrapperMobileNormalView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Wrapper Mobile Normal View'),
      ),
    );
  }
}
