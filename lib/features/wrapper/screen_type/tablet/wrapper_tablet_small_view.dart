import 'package:flutter/material.dart';

class WrapperTabletSmallView extends StatelessWidget {
  const WrapperTabletSmallView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Wrapper Tablet Small View'),
      ),
    );
  }
}
