import 'package:flutter/material.dart';

class WrapperTabletLargeView extends StatelessWidget {
  const WrapperTabletLargeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Wrapper Tablet Large View'),
      ),
    );
  }
}
