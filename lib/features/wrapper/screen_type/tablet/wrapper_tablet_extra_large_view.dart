import 'package:flutter/material.dart';

class WrapperTabletExtraLargeView extends StatelessWidget {
  const WrapperTabletExtraLargeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Wrapper Tablet Extra Large View'),
      ),
    );
  }
}
