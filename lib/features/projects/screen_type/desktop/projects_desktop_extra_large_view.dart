import 'package:flutter/material.dart';

class ProjectsDesktopExtraLargeView extends StatelessWidget {
  const ProjectsDesktopExtraLargeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Projects Desktop Extra Large View'),
      ),
    );
  }
}
