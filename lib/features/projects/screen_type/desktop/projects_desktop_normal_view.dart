import 'package:flutter/material.dart';

class ProjectsDesktopNormalView extends StatelessWidget {
  const ProjectsDesktopNormalView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Projects Desktop Normal View'),
      ),
    );
  }
}
