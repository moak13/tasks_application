import 'package:flutter/material.dart';

class ProjectsDesktopSmallView extends StatelessWidget {
  const ProjectsDesktopSmallView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Projects Desktop Small View'),
      ),
    );
  }
}
