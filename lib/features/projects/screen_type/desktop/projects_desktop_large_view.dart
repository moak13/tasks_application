import 'package:flutter/material.dart';

class ProjectsDesktopLargeView extends StatelessWidget {
  const ProjectsDesktopLargeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('projects Desktop Large View'),
      ),
    );
  }
}
