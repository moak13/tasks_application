import 'package:flutter/material.dart';

class ProjectsMobileSmallView extends StatelessWidget {
  const ProjectsMobileSmallView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Projects Mobile Small View'),
      ),
    );
  }
}
