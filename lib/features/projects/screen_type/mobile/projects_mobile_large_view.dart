import 'package:flutter/material.dart';

class ProjectsMobileLargeView extends StatelessWidget {
  const ProjectsMobileLargeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Projects Mobile Large View'),
      ),
    );
  }
}
