import 'package:flutter/material.dart';

class ProjectsMobileExtraLargeView extends StatelessWidget {
  const ProjectsMobileExtraLargeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Projects Mobile Extra Large View'),
      ),
    );
  }
}
