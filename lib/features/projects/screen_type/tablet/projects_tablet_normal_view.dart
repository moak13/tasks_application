import 'package:flutter/material.dart';

class ProjectsTabletNormalView extends StatelessWidget {
  const ProjectsTabletNormalView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Projects Tablet Normal View'),
      ),
    );
  }
}
