import 'package:flutter/material.dart';

class ProjectsTabletExtraLargeView extends StatelessWidget {
  const ProjectsTabletExtraLargeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Projects Tablet Extra Large View'),
      ),
    );
  }
}
