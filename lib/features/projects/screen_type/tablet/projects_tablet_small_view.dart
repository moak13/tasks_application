import 'package:flutter/material.dart';

class ProjectsTabletSmallView extends StatelessWidget {
  const ProjectsTabletSmallView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Projects Tablet Small View'),
      ),
    );
  }
}
