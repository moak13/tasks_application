import 'package:flutter/material.dart';

class ProjectsTabletLargeView extends StatelessWidget {
  const ProjectsTabletLargeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Projects Tablet Large View'),
      ),
    );
  }
}
